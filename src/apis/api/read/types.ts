// read 폴더에서 사용될 Type(Interface)
export interface getAgencyType {
    id?: string;
    name?: string;
}

export interface getProjectType {
    name: string;
    startDate: string;
    endDate: string;
}